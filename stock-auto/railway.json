{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "gunicorn app:app --config gunicorn.conf.py --bind 0.0.0.0:${PORT:-5000}",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}
